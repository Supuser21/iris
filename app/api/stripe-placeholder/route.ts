import { NextResponse } from "next/server";

/** Placeholder for post-beta Stripe integration */
export async function GET() {
  return NextResponse.json({
    status: "coming_soon",
    message: "Paid subscriptions will be available after beta.",
  });
}
